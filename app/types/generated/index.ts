/**
 * Stable barrel for ts-rs generated DTOs.
 *
 * `cargo run --manifest-path src-tauri/Cargo.toml --features typegen --bin gen_types`
 * emits one file per Rust struct into this directory. Each individual
 * `.ts` file is auto-regenerated by ts-rs; this `index.ts` is hand-maintained
 * to give callers a single import surface and to give the verify-gate
 * (`scripts/verify-gate.sh` "DTO drift" step) a stable name list to compare.
 *
 * Consumers should normally import from the owning shim file
 * (`~/types/config` / `~/types/marketplace`) so the public type names
 * keep working even if a generated file is renamed. Direct imports from
 * `./AppConfig` / `./GeneralConfig` / etc. are also supported.
 */

export type { AppConfig } from './AppConfig'
export type { BackupInfo } from './BackupInfo'
export type { GeneralConfig } from './GeneralConfig'
export type { GithubAccount } from './GithubAccount'
export type { LocalizedString } from './LocalizedString'
export type { LoggingConfig } from './LoggingConfig'
export type { MarketplaceEntry } from './MarketplaceEntry'
export type { MarketplaceIndex } from './MarketplaceIndex'
export type { MarketplaceInstallRequest } from './MarketplaceInstallRequest'
export type { SecurityConfig } from './SecurityConfig'
export type { ThemeUsage } from './ThemeUsage'
